overwrite: true
schema: "https://rickandmortyapi.com/graphql"
documents: "composables/**/*.ts"
generates:
  types/generated/graphql.ts:
    plugins:
      - "typescript"
  ./graphql.schema.json:
    plugins:
      - "introspection"
